# Optionnel — laissez ce fichier absent (ou vide) pour que l'app fonctionne
# entièrement en local (conversations et corrections scriptées).
#
# Pour brancher un vrai modèle de langage plus tard :
# 1. Déployez un petit backend/proxy qui détient la clé API Anthropic/OpenAI/etc.
#    (NE JAMAIS mettre une clé secrète directement dans le frontend).
# 2. Renseignez ici l'URL de ce proxy :
# VITE_AI_PROXY_URL=https://mon-backend.example.com/chat
